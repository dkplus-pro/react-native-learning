{
  "name": "react-native-learning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/course-structure.test.mjs",
    "lint": "node scripts/lint-course.mjs",
    "typecheck": "node scripts/typecheck-lessons.mjs",
    "verify": "npm run lint && npm run test && npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
